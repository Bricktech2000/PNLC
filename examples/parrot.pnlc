# vim:tw=80:ft=pnlc:
# inc:../prelude.pnlc

..def ..cons '?'u8 ..cons '\s'u8 nil \"?\s"
..def ..cons '>'u8 ..cons '\s'u8 nil \">\s"

.main .fix \main
  ...bindk io:mnad .puts "?\s"
    ...bind io:mnad getln \str
      ...bindk io:mnad .puts ">\s"
        ...bindk io:mnad .puts str
          main

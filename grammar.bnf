; keep in sync with pnlc.c, examples/pnlc.pnlc and pnlc.vim
<term> ::= "." <ws> <term> <term>            ; application
         | "\\" <ws> <var> <term>            ; abstraction
         | "#" <line> <ws> <term>            ; comment
         | <var> & (? head none of ".\\#" ?) ; variable
<line> ::= (? not '\n' ?)* "\n"
<var> ::= (? `!isspace` ?)+ (? `isspace` ?)+ ; assuming isspace(EOF)
<ws> ::= (? `isspace` ?)*
